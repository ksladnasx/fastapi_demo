## response_model与lifespan

### response_model

`response_model=UserRead` 的意思是：这个接口返回给前端的数据，按 `UserRead` 这个模型来整理和校验。同时还会做 类型转换/校验

比如：

```python
@router.get("/{user_id}", response_model=UserRead)
def get_user(user_id: int):
    user = User.get(user_id)  # 返回的是 User 对象（数据库模型）
    return user  # 但 FastAPI 会把它转换成 UserRead 格式
```

哪怕 `User.get(user_id)` 返回的是数据库里的 `User` 对象，FastAPI 最终响应时也会按 `UserRead` 输出。好处是：

```python
class UserRead(UserBase):
    id: int
    created_at: datetime
    updated_at: datetime | None = None
```

所以接口只会返回 `UserRead` 里定义的字段。你可以把它理解成“响应数据的格式声明”。同时它也会体现在 `/docs` 文档里，让 Swagger 知道这个接口返回什么结构。

实际发生的事情：

```
User.get(user_id) 
    ↓ 返回 User 对象（包含所有字段：id, username, email, password, full_name, is_active, created_at, updated_at）
    ↓
FastAPI 看到 response_model=UserRead
    ↓ 自动做转换：只保留 UserRead 中定义的字段
    ↓ 过滤掉 UserRead 中没有的字段（如 password）
    ↓
返回给前端的是干净的 UserRead 格式
```

### lifespan

`lifespan=lifespan` 是 FastAPI 的应用生命周期钩子。

你现在的代码大概是：

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    print("Starting up...")
    init_db()
    print("Database initialized")
    yield
    print("Shutting down...")
```

意思是：

```text
启动项目时：
    执行 yield 前面的代码

项目运行中：
    停在 yield 这里

关闭项目时：
    执行 yield 后面的代码
```

所以你这里的作用是：FastAPI 启动时自动执行 `init_db()`，也就是根据 SQLModel 的模型创建表。

简单说：

```python
app = FastAPI(lifespan=lifespan)
```

就是告诉 FastAPI：

> 这个应用启动和关闭时，按我定义的 `lifespan()` 函数处理。

在你这个项目里，主要用途就是启动时初始化数据库表。

## get_session()函数区别


这两个函数本质上都在做同一件事：创建一个数据库 `Session`，用完自动关闭。

区别在于：**给谁用、怎么用**。

`get_session()` 是给 FastAPI 依赖注入用的：

```python
def get_session():
    with Session(engine) as session:
        yield session
```

典型用法是：

```python
def create_user(session: Session = Depends(get_session)):
    ...
```

FastAPI 会自动调用它，把 `session` 注入到接口函数里。请求结束后，FastAPI 会自动处理 `yield` 后的清理逻辑。

---

`get_sync_db_session()` 是给普通 Python 代码手动 `with` 使用的：

```python
@contextmanager
def get_sync_db_session() -> Generator[Session]:
    with Session(engine) as session:
        yield session
```

典型用法是：

```python
with get_sync_db_session() as session:
    user = session.get(User, user_id)
```

它不是给 `Depends()` 用的，而是给你现在这种写法用的：在 `models/user.py` 的类方法里面自己打开数据库会话。

---

你现在已经不在路由里注入 `session` 了，所以 `get_session()` 其实可以删掉，只保留：

```python
@contextmanager
def get_sync_db_session() -> Generator[Session, None, None]:
    with Session(engine) as session:
        yield session
```

然后模型里统一用：

```python
with get_sync_db_session() as session:
    ...
```

简单总结：

```text
get_session          给 FastAPI Depends 用
get_sync_db_session  给普通 with 语句用
```

按你当前项目结构，只需要 `get_sync_db_session()`。

## statement链式调用

代码中对数据库的操作有类似语句：

```py
def list(cls, page: int = 1,page_size: int = 10,is_active: bool | None = None, ) -> list[User]:
        with get_sync_db_session() as session:
            statement = select(User)
            if is_active is not None:
                statement = statement.where(User.is_active == is_active)
            offset = (page - 1) * page_size #计算偏移量，表示从哪条记录开始
            statement = statement.offset(offset).limit(page_size)
            return list(session.exec(statement).all())
```

其中的statement被赋值了多次，实际上每次都是重新给他赋值，相当于给对象又加属性。这就是为什么你可以写成**链式调用**：

```python
# 方式1：分开写（每次重新赋值）
statement = select(User)
statement = statement.where(User.is_active == is_active)
statement = statement.offset(offset).limit(page_size)

# 方式2：链式调用（每次返回新对象）
statement = (
    select(User)
    .where(User.is_active == is_active)
    .offset(offset)
    .limit(page_size)
)
```

两种方式完全等价！链式调用的本质就是：

1. `select(User)` 返回一个对象
2. `.where()` 返回一个新对象（包含 where 条件）
3. `.offset()` 返回一个新对象（包含 offset）
4. `.limit()` 返回一个新对象（包含 limit）

这种设计叫做**不可变模式（Immutable Pattern）**，好处是多个请求可以共用基础查询，不会互相影响。而且可以通过复用基础查询来构建多个不同的查询：

```py
# 构建基础查询，base 始终保持不变 ✅
base = select(User).where(User.is_active == True)

# 复用它来排序
sorted_query = base.order_by(User.created_at.desc())

# 复用它来分页
paginated_query = base.offset(0).limit(10)
```

## CORS 中间件

这里的“中间件”主要是指我在 `app/main.py` 里加的 CORS 中间件：

```
from fastapi.middleware.cors import CORSMiddleware
```

然后在创建 `FastAPI` 应用后，注册到 `app` 上：

```
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**它解决的问题**

前端页面是从浏览器打开的，比如：

```
file:///D:/Code/backend_demo/fastapi_demo/frontend/index.html
```

或者未来可能是：

```
http://127.0.0.1:5500
```

而后端接口是：

```
http://127.0.0.1:8000
```

浏览器认为这两个来源不同，就会触发跨域限制。即使你的后端接口本身没问题，浏览器也可能先把请求拦下来，不让前端拿到响应。

CORS 中间件的作用就是：在后端响应里自动加上一些浏览器认可的跨域响应头，告诉浏览器“这个前端页面可以访问我”。

**代码执行位置**

当前结构大概是：

```
app = FastAPI(...)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

register_exception_handlers(app)
app.include_router(users.router)
```

也就是说，FastAPI 应用创建完成后，先把 CORS 中间件挂上去，再注册异常处理器和路由。

中间件挂上之后，请求进入后端时，流程可以理解成：

```
浏览器请求
  -> CORSMiddleware
    -> FastAPI 路由
      -> /user/login 或 /user/me 等接口
    <- 接口响应
  <- CORSMiddleware 给响应补跨域头
浏览器收到响应
```

**几个参数的含义**

```
allow_origins=["*"]
```

表示允许所有来源访问后端接口。比如：

```
http://127.0.0.1:5500
http://localhost:5173
file://...
```

本地测试时这样最方便。但正式项目里不建议用 `*`，应该改成明确的前端地址，比如：

```
allow_origins=["http://127.0.0.1:5173", "http://localhost:5173"]
```

`allow_methods=["*"]`表示允许所有 HTTP 方法跨域访问。例如POST和GET

`allow_headers=["*"]` 表示允许前端携带任意请求头。

这对 JWT 测试很关键，因为前端会发：

```
Authorization: Bearer your_access_token
```

如果没有允许 `Authorization` 这个请求头，浏览器可能会在预检请求阶段就拒绝正式请求。

**为什么会有 OPTIONS 请求**

当前前端带了两个比较关键的东西：

```
headers.Authorization = `Bearer ${getToken()}`;
headers["Content-Type"] = "application/json";
```

浏览器发现这是一个“非简单请求”，可能会先自动发送一个 `OPTIONS` 预检请求，问后端：

```
我等会儿要发 POST/GET，还要带 Authorization 请求头，可以吗？
```

CORS 中间件会自动处理这个 `OPTIONS` 请求，并返回类似这些响应头：

```
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: DELETE, GET, HEAD, OPTIONS, PATCH, POST, PUT
Access-Control-Allow-Headers: Authorization, Content-Type, ...
```

浏览器看到允许之后，才会继续发送真正的 `/user/login`、`/user/me` 请求。

**和 JWT 认证依赖的关系**

CORS 中间件和 JWT 认证不是一回事。

CORS 负责的是：

```
浏览器是否允许前端页面跨域访问后端
```

JWT 认证负责的是：

```
后端是否认可这个请求里的 access_token
```

它们的顺序可以理解成：

```
1. 浏览器准备请求后端
2. 如果跨域，先走 CORS 预检
3. CORS 允许后，浏览器发送真实请求
4. 请求进入 FastAPI
5. 受保护接口触发 CurrentUserDep
6. CurrentUserDep 读取 Authorization 请求头
7. decode_access_token 校验 JWT
8. 校验通过，接口继续执行
```

所以 CORS 是浏览器层面的通行许可，JWT 是业务层面的身份认证。

**当前实现的特点**

现在这段配置偏向本地开发测试：

```
allow_origins=["*"]
allow_methods=["*"]
allow_headers=["*"]
```

优点是简单，前端页面怎么打开都比较容易请求成功。

缺点是权限太宽，正式环境不够严谨。正式环境更建议写成：

```
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://你的前端域名.com"],
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["Authorization", "Content-Type"],
)
```

这样只允许指定前端域名、指定方法、指定请求头访问后端，会更安全。当前项目是 demo 和本地测试，所以用宽松配置更方便。

## 注册异常处理器

`exceptions.py` 这里做的是“统一业务异常响应”。

核心目的：业务代码里不用到处手写 `JSONResponse`，只要抛出自定义异常，比如 `BadRequestException("邮箱已被注册")`，FastAPI 就会自动把它转换成统一格式的 JSON 返回给前端。

代码结构大概分三层。

第一层是基础异常类：

```py
class AppException(Exception):
    status_code = status.HTTP_400_BAD_REQUEST

    def __init__(self, detail: str) -> None:
        self.detail = detail
```

`AppException` 继承 Python 内置的 `Exception`类。
它保存两个东西：`status_code` 和`detail`

`status_code` 表示 HTTP 状态码，默认是 `400`。`detail` 表示具体错误信息，比如：

```
"用户名已存在"
"User not found"
"Invalid or expired access token"
```

第二层是具体业务异常：

```py
class BadRequestException(AppException):
    status_code = status.HTTP_400_BAD_REQUEST


class NotFoundException(AppException):
    status_code = status.HTTP_404_NOT_FOUND


class UnauthorizedException(AppException):
    status_code = status.HTTP_401_UNAUTHORIZED
```

这些类继承自`AppException`类，本身没有重写逻辑，只是换了不同的 `status_code`。

`raise BadRequestException("邮箱已被注册")`最终会返回 HTTP `400`。

`raise NotFoundException("User not found")`最终会返回 HTTP `404`。

`raise UnauthorizedException("Invalid access token")`最终会返回 HTTP `401`。

第三层是注册异常处理器：

```py
def register_exception_handlers(app: FastAPI) -> None:
    @app.exception_handler(AppException)
    async def app_exception_handler(
        request: Request,
        exc: AppException,
    ) -> JSONResponse:
        return JSONResponse(
            status_code=exc.status_code,
            content={
                "code": exc.status_code,
                "data": None,
                "message": exc.detail,
            },
        )
```

这段最关键。它告诉 FastAPI：只要接口执行过程中抛出了 `AppException` 或它的子类，就不要让程序直接报错，也不要返回默认错误格式，而是统一走这里。

比如业务层写：

```py
if UserDao.get_by_email(email):
    raise BadRequestException("邮箱已被注册")
```

FastAPI 捕获到这个异常后，会进入 `app_exception_handler()`，然后返回：

```json
{
  "code": 400,
  "data": null,
  "message": "邮箱已被注册"
}
```

再比如 JWT 校验失败：

```py
raise UnauthorizedException("Invalid or expired access token")
```

返回：

```json
{
  "code": 401,
  "data": null,
  "message": "Invalid or expired access token"
}
```

整体调用链可以理解成：

```
接口函数
  -> service 业务逻辑
    -> 发现错误
    -> raise BadRequestException(...)
  -> FastAPI 捕获异常
  -> app_exception_handler()
  -> 返回统一 JSON
```

这个文件的价值在于：成功响应和失败响应风格一致。

成功响应现在是：

```json
{
  "code": 0,
  "data": {},
  "message": "success"
}
```

业务异常响应是：

```json
{
  "code": 400,
  "data": null,
  "message": "具体错误原因"
}
```

所以前端处理起来会很舒服：统一看 `code`、`data`、`message`。
